# hub repository contains tab-completion scripts for bash and zsh.
# These scripts complement existing completion scripts that ship with git.

if [ -f /usr/local/etc/bash_completion.d/hub.bash_completion.sh ]; then
    . /usr/local/etc/bash_completion.d/hub.bash_completion.sh
fi
